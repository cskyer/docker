server {
  listen 80 default_server;
  listen [::]:80 default_server;
  server_name _;

  access_log /var/log/nginx/default.access.log main;
  error_log /var/log/nginx/default.error.log warn;

  location = /healthz {
    access_log off;
    add_header Content-Type text/plain;
    return 200 "ok\n";
  }

  location / {
    return 404;
  }

  error_page 500 502 503 504 /50x.html;

  location = /50x.html {
    root /usr/share/nginx/html;
    internal;
  }
}

# Example HTTP reverse proxy server.
# Copy this block into a new file such as conf.d/app.example.com.conf,
# then update server_name and proxy_pass to match the upstream service.
#
# server {
#   listen 80;
#   listen [::]:80;
#   server_name app.example.com;
#
#   location / {
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection $connection_upgrade;
#     proxy_pass http://app:8080;
#   }
# }
#
# Example HTTPS server.
# Place certificates in ./certs and keep private keys out of git.
#
# server {
#   listen 443 ssl http2;
#   listen [::]:443 ssl http2;
#   server_name app.example.com;
#
#   ssl_certificate /etc/nginx/certs/app.example.com.crt;
#   ssl_certificate_key /etc/nginx/certs/app.example.com.key;
#   ssl_protocols TLSv1.2 TLSv1.3;
#   ssl_prefer_server_ciphers off;
#
#   location / {
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto https;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection $connection_upgrade;
#     proxy_pass http://app:8080;
#   }
# }
